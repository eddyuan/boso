import { DesignGallery } from "../_components/design-gallery";

export const metadata = {
  title: "App design — Tielo Admin",
};

export default function AppDesignPage() {
  return <DesignGallery />;
}
