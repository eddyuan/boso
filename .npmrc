# Metro (React Native's bundler) doesn't resolve pnpm's symlinked node_modules
# well in a monorepo. Hoisting keeps node_modules flat like npm/yarn classic,
# which is the path of least resistance for Expo + pnpm workspaces.
node-linker=hoisted
